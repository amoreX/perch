## Todo

-  Please make it so that it is draggable, the widgets that are draggable and adjustable are even resizable as well but at every size and resize position that you can think of, or make it up to be, should be properly padded and everything should be properly visible. Make sure of that and they should be draggable and everything. Add an edit icon or something so that it's draggable, I guess, and there should be a drag state so that the entire notch is visible. The user can drag it up and down, the stuff in the widget, and it shows a line which shows the visible area without scrolling. Just make sure that works properly.

Maybe we need to implement some kind of matrix that shows a user a grid sort of view so that is there. Also make sure that just that is there: remove the Claude Code part from the agents view. There is no need to show the Claude Code. Only show schedule stuff, their schedule tasks, and just the user history, like chat history.

Also there should be a schedule task widget in the main page so that is there. It should be toggleable from the settings as well. These are the stuff.

